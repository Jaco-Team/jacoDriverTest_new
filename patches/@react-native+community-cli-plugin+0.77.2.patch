diff --git a/node_modules/@react-native/community-cli-plugin/dist/commands/bundle/buildBundle.js b/node_modules/@react-native/community-cli-plugin/dist/commands/bundle/buildBundle.js
index 97157a5..e7d1d8b 100644
--- a/node_modules/@react-native/community-cli-plugin/dist/commands/bundle/buildBundle.js
+++ b/node_modules/@react-native/community-cli-plugin/dist/commands/bundle/buildBundle.js
@@ -11,16 +11,17 @@ var _parseKeyValueParamArray = _interopRequireDefault(
 var _saveAssets = _interopRequireDefault(require("./saveAssets"));
 var _chalk = _interopRequireDefault(require("chalk"));
 var _fs = require("fs");
-var _Server = _interopRequireDefault(require("metro/src/Server"));
-var _bundle = _interopRequireDefault(require("metro/src/shared/output/bundle"));
-var _RamBundle = _interopRequireDefault(
-  require("metro/src/shared/output/RamBundle")
-);
 var _path = _interopRequireDefault(require("path"));
 function _interopRequireDefault(e) {
   return e && e.__esModule ? e : { default: e };
 }
-async function buildBundle(_argv, ctx, args, bundleImpl = _bundle.default) {
+function getMetroServer() {
+  return _interopRequireDefault(require("metro/src/Server")).default;
+}
+function getMetroBundle() {
+  return _interopRequireDefault(require("metro/src/shared/output/bundle")).default;
+}
+async function buildBundle(_argv, ctx, args, bundleImpl = getMetroBundle()) {
   const config = await (0, _loadMetroConfig.default)(ctx, {
     maxWorkers: args.maxWorkers,
     resetCache: args.resetCache,
@@ -33,7 +34,7 @@ async function buildBundle(_argv, ctx, args, bundleImpl = _bundle.default) {
 async function buildBundleWithConfig(
   args,
   config,
-  bundleImpl = _bundle.default
+  bundleImpl = getMetroBundle()
 ) {
   const customResolverOptions = (0, _parseKeyValueParamArray.default)(
     args.resolverOption ?? []
@@ -68,7 +69,8 @@ async function buildBundleWithConfig(
     unstable_transformProfile: args.unstableTransformProfile,
     customResolverOptions,
   };
-  const server = new _Server.default(config);
+  const Server = getMetroServer();
+  const server = new Server(config);
   try {
     const bundle = await bundleImpl.build(server, requestOpts);
     await _fs.promises.mkdir(_path.default.dirname(args.bundleOutput), {
@@ -77,7 +79,7 @@ async function buildBundleWithConfig(
     });
     await bundleImpl.save(bundle, args, console.info);
     const outputAssets = await server.getAssets({
-      ..._Server.default.DEFAULT_BUNDLE_OPTIONS,
+      ...Server.DEFAULT_BUNDLE_OPTIONS,
       ...requestOpts,
       bundleType: "todo",
     });
diff --git a/node_modules/@react-native/community-cli-plugin/dist/commands/start/runServer.js b/node_modules/@react-native/community-cli-plugin/dist/commands/start/runServer.js
index cbafad7..09cb29a 100644
--- a/node_modules/@react-native/community-cli-plugin/dist/commands/start/runServer.js
+++ b/node_modules/@react-native/community-cli-plugin/dist/commands/start/runServer.js
@@ -16,9 +16,6 @@ var version = _interopRequireWildcard(require("../../utils/version"));
 var _attachKeyHandlers = _interopRequireDefault(require("./attachKeyHandlers"));
 var _middleware = require("./middleware");
-var _devMiddleware = require("@react-native/dev-middleware");
 var _chalk = _interopRequireDefault(require("chalk"));
-var _metro = _interopRequireDefault(require("metro"));
-var _metroCore = require("metro-core");
 var _path = _interopRequireDefault(require("path"));
 var _url = _interopRequireDefault(require("url"));
 function _getRequireWildcardCache(e) {
@@ -46,5 +43,14 @@ function _interopRequireDefault(e) {
   return e && e.__esModule ? e : { default: e };
 }
+function getMetro() {
+  return _interopRequireDefault(require("metro")).default;
+}
+function getMetroCore() {
+  return require("metro-core");
+}
+function getDevMiddleware() {
+  return require("@react-native/dev-middleware");
+}
 async function runServer(_argv, cliConfig, args) {
   const metroConfig = await (0, _loadMetroConfig.default)(cliConfig, {
     config: args.config,
@@ -96,7 +102,8 @@ async function runServer(_argv, cliConfig, args) {
     metroConfig.server.forwardClientLogs = false;
   }
   let reportEvent;
-  const terminal = new _metroCore.Terminal(process.stdout);
+  const { Terminal } = getMetroCore();
+  const terminal = new Terminal(process.stdout);
   const ReporterImpl = getReporterImpl(args.customLogReporterPath);
   const terminalReporter = new ReporterImpl(terminal);
   const {
@@ -108,9 +115,9 @@ async function runServer(_argv, cliConfig, args) {
     host: hostname,
     port,
     watchFolders,
   });
-  const { middleware, websocketEndpoints } = (0,
-  _devMiddleware.createDevMiddleware)({
+  const { createDevMiddleware } = getDevMiddleware();
+  const { middleware, websocketEndpoints } = createDevMiddleware({
     projectRoot,
     serverBaseUrl: devServerUrl,
     logger: (0, _createDevMiddlewareLogger.default)(terminalReporter),
@@ -140,3 +147,3 @@ async function runServer(_argv, cliConfig, args) {
   };
-  const serverInstance = await _metro.default.runServer(metroConfig, {
+  const serverInstance = await getMetro().runServer(metroConfig, {
     host: args.host,
