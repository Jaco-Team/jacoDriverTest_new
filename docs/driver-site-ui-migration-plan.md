# План переноса UI курьерского сайта в приложение

Дата создания: 27 августа 2026 года. Последнее обновление: 4 сентября 2026 года.

Ветка приложения: `new_architecture`. Ветка `main` этой работой не меняется.

Эталон:

- проект `jaco_driver_site`;
- ветка `api-laravel`;
- текущее состояние всей ветки `api-laravel`, а не отдельный зафиксированный коммит;
- рабочий сайт: [driver.jacochef.ru](https://driver.jacochef.ru/auth).

Перед каждым UI-этапом необходимо проверить, что `jaco_driver_site` находится
на ветке `api-laravel`, и заново изучить актуальные компоненты, стили, темы,
типы, API-контракты и связанные файлы этой ветки. Исторические коммиты, старые версии
файлов и ранее выписанные значения не используются как эталон.

Этот документ ведёт UI-миграцию отдельно от обновления React Native / Fabric. Технический фундамент и его ограничения зафиксированы в [docs/new-architecture-migration-plan.md](./new-architecture-migration-plan.md).

Перед началом экранной миграции фундамент зафиксирован на React Native `0.87.1`; совместимые зависимости обновлены внутри текущих major и проверены через lint, typecheck, unit, Android Debug и iOS Debug. Tailwind `3.4.19` и NativeWind `4.2.6` на этом этапе не менялись.

## 1. Цель

Перенести новый интерфейс курьерского сайта в React Native-приложение максимально близко визуально, но с корректной адаптацией под Android и iOS.

Это тот же курьерский продукт, а не новая бизнес-логика приложения.

Основные правила:

- переносить UI поэтапно, по одному согласованному экрану или связанному блоку;
- до начала каждого этапа фиксировать `In scope now`, `Out of scope` и критерии приёмки;
- код менять только после отдельного разрешения пользователя на конкретный этап;
- сохранять текущие API, store, токен, навигацию и аналитику, если их изменение отдельно не согласовано;
- не копировать MUI, SCSS и web-реализации буквально — переносить визуальный результат средствами React Native;
- не придумывать форматы данных, тексты, размеры, цвета, состояния или поведение: каждое значение должно подтверждаться актуальным проектом `jaco_driver_site` в ветке `api-laravel`;
- тестовые данные для визуальной проверки должны повторять форматы актуальных типов и API-контрактов сайта, а не создаваться произвольно;
- тесты сайта не копируются и не используются как шаблон; приложение сохраняет собственные unit/UI-тесты, соответствующие React Native и текущей мобильной логике;
- если нужное значение невозможно подтвердить по актуальному проекту или рабочему сайту, сначала зафиксировать неопределённость и согласовать её с пользователем;
- не добавлять новую зависимость без отдельного согласования;
- после этапа обновлять статус в этом документе;
- коммиты делает только пользователь.

## 2. Зафиксированные решения

### SSO и CAPTCHA

Первоначальные визуальные заглушки заменены полноценной интеграцией после перехода
на Laravel API. SmartCaptcha получает и передаёт настоящий token при входе и
восстановлении пароля. SSO использует системную auth-сессию, фиксированный deep
link и обмен одноразового `login_code` на Bearer-токен; сценарий вручную проверен
на iOS Simulator и Android Emulator 04.09.2026. Подробный актуальный статус
зафиксирован в [плане подключения к Laravel API](./laravel-api-mobile-migration-plan.md).

### Решение по сохранению данных для входа

Отдельное сохранение логина и пароля по примеру `jaco_test_app_site_new`
исключено из плана 04.09.2026. Повторный вход выполняется через SSO с серверным
«Запомнить меня»; приложение хранит только access token и не сохраняет пароль.

### Визуальное соответствие

Эталоном является мобильный вид сайта, а не его desktop-компоновка. Для каждого экрана учитываются:

- светлое и тёмное состояния, если они есть на сайте;
- начальное, заполненное, loading, empty и error-состояния;
- размеры, отступы, радиусы, цвета, тени, типографика и иконки;
- клавиатура, прокрутка, Safe Area, Status Bar и нижняя системная область;
- допустимые различия нативных контролов Android и iOS.

Допустимое отличие от сайта должно быть записано в разделе соответствующего этапа, а не приниматься молча.

### Бизнес-поведение

Визуальный перенос сам по себе не разрешает:

- менять формат запросов авторизации;
- переносить web cookie-session в мобильное приложение;
- переписывать глобальный store;
- менять правила действий с заказами;
- менять GPS-условия;
- менять карту или навигацию за пределами согласованного UI-этапа.

### SMS-код и Laravel backend

Мобильное приложение переведено на REST API `https://apidriver.jacochef.ru/api/v1/...`.
Восстановление пароля использует Laravel endpoints и шестизначный SMS-код; legacy
`get_sms/check_code` больше не используется. Пункт «Статистика» отображается только
по backend-флагу `driver_page_stat_time`, временное условие через `__DEV__` удалено.

Что выполнено на этапе подключения нового backend:

Полный порядок работ, локальный запуск и критерии совместимости вынесены в
[план подключения мобильного приложения к Laravel API](./laravel-api-mobile-migration-plan.md).

- [x] legacy-вызовы `auth → get_sms/check_code` заменены REST-маршрутами нового `apidriver`;
- [x] контракты входа, восстановления пароля, токена, ошибок и CAPTCHA перенесены;
- [x] визуальная CAPTCHA заменена полноценной SmartCaptcha в авторизации и восстановлении;
- [x] визуальная кнопка SSO заменена системной auth-сессией с OIDC/PKCE на Laravel,
  deep link и обменом одноразового кода на токен;
- [x] ограничение и валидация SMS-кода изменены с 4 на 6 цифр;
- [ ] реальная доставка и подтверждение SMS проверяются руководителем на существующем номере;
- [x] `driver_page_stat_time`, пункт «Статистика» и переход на экран проверены;
- [x] в «Настройки» добавлен настоящий выбор кафе по `point_id/all_points` с сохранением;
- [x] временные dev-логи `[AUTH-RECOVERY]` удалены.

## 3. Базовые параметры эталона авторизации

Мобильный эталон проверен при viewport `390 × 844`:

- внешние отступы: `14–16`;
- карточка: радиус `24`, внутренние отступы `24 × 20`;
- логотип: около `84 × 84`;
- заголовок: `32`, weight `800`;
- поля: высота `58`, радиус `18`;
- кнопки: высота `52`, радиус `18`;
- основной градиент: `#CC0033 → #A6002A`;
- фон: светлый серо-белый градиент с мягкими декоративными пятнами.

Логотип сайта и [src/shared/assets/logo.png](../src/shared/assets/logo.png) бинарно совпадают. Старый [src/shared/assets/wallpaper.png](../src/shared/assets/wallpaper.png) не является частью нового UI авторизации.

Эти значения — исходная точка для нативной адаптации, а не требование использовать CSS-пиксели напрямую.

## 4. Порядок этапов

### Этап 0. Документация и роли

Статус: `завершён`.

- создать этот план;
- связать его с общей документацией и планом Fabric;
- расширить существующие роли Cursor под поэкранный UI-процесс;
- не создавать отдельную UI-роль без необходимости;
- код приложения не менять.

### Этап 1. Авторизация и запуск

Статус: `UI и мобильная интеграция Laravel завершены`; production-настройка
CAPTCHA/SSO и проверка реального SMS остаются внешними задачами.

Внутренний порядок:

1. [src/features/auth/ui/AuthScreen.tsx](../src/features/auth/ui/AuthScreen.tsx) — завершён и визуально принят владельцем проекта; SSO работает через системную auth-сессию и deep link, SmartCaptcha показывается по ответу `captcha_required`.
2. [src/features/reset-pwd/ui/ResetPwdScreen.tsx](../src/features/reset-pwd/ui/ResetPwdScreen.tsx) — перенесены два шага, требования пароля, реальная SmartCaptcha, inline-состояния и шестизначный SMS-код Laravel. До получения кода форма без CAPTCHA не отправляется; реальную доставку SMS проверяет руководитель на существующем рабочем номере.
3. [src/app/screens/Greeting.tsx](../src/app/screens/Greeting.tsx) — визуально проверен; старый `wallpaper.png` удалён, перенесены красный фон, крупный логотип и белая типографика `/initial`. Startup использует Bearer-токен из Keychain/Keystore и корректно снимает состояние загрузки при ошибке.

Задачи этапа:

- перенести композицию, фон, карточку, логотип, заголовки и тексты;
- перенести поля и переключатель видимости пароля;
- перенести hint и визуальное error-состояние;
- добавить функциональные SSO и SmartCaptcha после перехода на Laravel API;
- хранить только Bearer-токен в защищённом хранилище, не сохранять логин и пароль;
- адаптировать экран под клавиатуру и небольшую высоту;
- настроить Safe Area и Status Bar для экранов без drawer-шапки;
- добавить доступные названия и стабильные test ID там, где они нужны проверкам.

Изменения этапа выполнены по отдельным разрешениям. Мобильная часть Laravel-интеграции
готова; развёртывание server-side изменений в production выполняет ответственный за сервер.

### Этап 2. Общий интерфейс

Статус: `завершён в текущем светлом UI`; итоговая проверка системных областей остаётся в этапе 6.

Внутренний порядок:

1. Минимальная палитра общего каркаса — реализована в [src/shared/styles/appPalette.ts](../src/shared/styles/appPalette.ts) на основе `appPalette` сайта.
2. Шапка — реализована в [src/app/navigation/MainDrawerNavigator.tsx](../src/app/navigation/MainDrawerNavigator.tsx) и визуально проверена владельцем проекта на Android и iOS.
3. Боковое меню — реализовано в [src/app/navigation/CustomDrawerContent.tsx](../src/app/navigation/CustomDrawerContent.tsx) и визуально проверено владельцем проекта на Android и iOS.
4. Общие поверхности и `ScreenLayout` дорабатываются далее вместе с каждым переносимым экраном, чтобы не менять ещё не согласованные страницы заранее.
5. Полная светлая/тёмная тема вынесена в отдельную задачу после появления готового эталона на сайте.

Основные точки:

- [src/app/navigation/MainDrawerNavigator.tsx](../src/app/navigation/MainDrawerNavigator.tsx)
- [src/app/navigation/CustomDrawerContent.tsx](../src/app/navigation/CustomDrawerContent.tsx)
- [src/app/providers/UiProviders.tsx](../src/app/providers/UiProviders.tsx)
- [src/app/providers/AppProviders.tsx](../src/app/providers/AppProviders.tsx)
- [src/shared/ui/ScreenLayout.tsx](../src/shared/ui/ScreenLayout.tsx)

Задачи этапа:

- общий фон и поверхности;
- палитра и минимальный набор переиспользуемых UI-токенов;
- шапка и боковое меню;
- единое поведение Safe Area и Status Bar;
- отсутствие лишних изменений навигационного поведения.

### Отложенная задача. Светлая и тёмная темы после нового backend

Статус: `отложена до появления готовой светлой/тёмной темы на сайте`.

Причины вынесения в отдельную задачу:

- интерфейс сайта пока не даёт готового и принятого эталона обеих тем для переноса;
- существующее поле приложения `theme` отвечает за оформление маркеров карты, а `night_map` — только за ночной режим карты, поэтому использовать их для темы всего приложения нельзя;
- временная самостоятельная тема мобильного приложения разошлась бы с будущим эталоном сайта.

Перед началом отдельной задачи необходимо:

- получить и принять реализацию светлой/тёмной темы в актуальной ветке сайта;
- подтвердить контракт чтения и сохранения `dark_theme`;
- добавить отдельное состояние темы приложения, не связанное с темой маркеров и `night_map`;
- подключить режим к Gluestack, общей палитре, навигации, Safe Area и Status Bar;
- переносить тёмные состояния поэкранно и проверить их на Android и iOS.

### Этап 3. Заказы

Статус: `завершён в согласованном объёме`; платёжный QR и неподтверждённая реализация группировки маркеров остаются отдельными отложенными вопросами.

Внутренний порядок:

1. список заказов — реализован и проверен владельцем проекта в debug; перенесены сводка лимитов, селектор типа, loading, empty и pull-to-refresh в [src/features/orders-list/ui/OrdersListScreen.tsx](../src/features/orders-list/ui/OrdersListScreen.tsx), [src/features/orders-list/ui/OrdersList.tsx](../src/features/orders-list/ui/OrdersList.tsx), [src/features/orders-list/ui/TypeLimit.tsx](../src/features/orders-list/ui/TypeLimit.tsx) и [src/features/orders-list/ui/OrdersTypeList.tsx](../src/features/orders-list/ui/OrdersTypeList.tsx); фильтр активных статусов перенесён на общий `Actionsheet` в [src/features/orders-map/ui/ModalFilterOrders.tsx](../src/features/orders-map/ui/ModalFilterOrders.tsx);
2. карточка заказа — реализована и проверена в debug; перенесены контейнер, типографика, теги, информационные строки, оплата и отменённое состояние в [src/entities/CardOrder/ui/CardOrder.tsx](../src/entities/CardOrder/ui/CardOrder.tsx); один телефон вызывает звонок, несколько открывают нижнюю шторку выбора;
3. кнопки действий — реализованы по актуальному `OrderCard.tsx`; платёжный QR остаётся отдельным неподтверждённым сценарием и в текущую сдачу не входит;
4. подтверждения действий — реализованы по актуальному `OrderConfirmModal.tsx`: тексты, иконки, цвета, нижняя шторка, безопасный нижний отступ, блокировка и индикатор выполнения сохранены;
5. карта — согласованный UI реализован и проверен в debug и release на Samsung. Сохранены нативные элементы приложения. Для Android/Fabric стабилизирован маркер курьера: нативный дочерний `YaMap` остаётся смонтированным, поэтому повторное определение позиции работает без перестановки native children. Локальный spinner вместо иконки геолокации удалён; блокировка действий остаётся на общем spinner.

Группировка нескольких заказов в одной координате и указатели заказов за границами видимой области пока не переносятся: актуальная чистая ветка `api-laravel` импортирует `orderMapGroups` и `mapViewport`, но соответствующие файлы отсутствуют в проекте. Реализацию нельзя воспроизводить предположительно; подпункт возвращается в scope после появления полного эталонного кода.

Для каждого подпункта отдельно проверяются loading, empty, error и рабочие состояния. Бизнес-правила заказов, GPS и API не меняются без отдельного scope.

### Отложенный вопрос к руководителю. Платёжная QR-кнопка

Статус: `обсудить с руководителем перед завершением UI-миграции`.

В текущем мобильном приложении и доступной Git-истории отсутствуют законченные
обработчик QR-кнопки, платёжная модалка и проверка оплаты. Laravel endpoints существуют,
но рабочий QR-сценарий сейчас не подтверждён ни в приложении, ни на сайте.

Необходимо получить решение руководителя:

1. переносить в приложение полный платёжный QR-сценарий вместе с новым backend;
2. временно скрыть неработающую QR-кнопку до появления поддерживаемого API.

До решения руководителя функциональность QR не реализовывать и не придумывать поведение,
даже несмотря на наличие Laravel endpoints: рабочий и принятый сценарий отсутствует и на сайте.

### Этап 4. Расчёт, график и статистика

Статус: `реализован и проверен владельцем проекта в debug`.

Внутренний порядок:

1. расчёт — реализован по актуальным `PriceScreen.tsx`, `MetricRow.tsx`, `usePriceScreen.ts` и `price.scss` сайта; данные загружаются через Laravel `/price/between`;
2. график работы — реализован по актуальным компонентам сайта; Laravel API используется для графика и обжалований, просмотр изображений и аналитика сохранены;
3. статистика — реализована по актуальным `StatisticsScreen.tsx`, `useStatisticsScreen.ts` и `price.scss` сайта; перенесены диапазон за последние семь дней, единый селектор периода, ограничение до 93 дней, кнопка «Показать статистику», карточки курьеров, итоговая карточка, показатели и empty-состояние. Данные загружаются через Laravel API, а пункт меню зависит только от backend-флага `driver_page_stat_time`.

Каждая страница является отдельным согласуемым подпунктом.

### Этап 5. Настройки и обратная связь

Статус: `настройки и обратная связь реализованы и проверены владельцем проекта в debug`.

Внутренний порядок:

1. настройки — реализованы по актуальному UI сайта и переведены на Laravel API. Выбор кафе использует `all_points`, отображает «город, точка», открывает ограниченную по высоте нижнюю шторку со скроллом, разделителями и выделением выбранного пункта; очистка доступна из сфокусированного поля. Сохранение синхронизирует settings store и интервал обновления заказов;
2. обратная связь — реализована по актуальным компонентам сайта и Laravel API; сохранены фильтрация, загрузка изображений и аналитика. Чекбокс «Уведомить о решении» работает как boolean-состояние.

Исправления чекбоксов и синхронизации store выполнены в согласованном scope страницы настроек.

### Этап 6. Системные области и финальная стабилизация

Статус: `код стабилизирован; debug smoke Android/iOS и release smoke Samsung/iOS Simulator выполнены`.

- итоговая проверка Safe Area и Status Bar на всех экранах — код унифицирован: корневой `SafeAreaProvider`, drawer-экраны без повторного верхнего inset, отдельные полноэкранные auth-экраны со всеми краями, единый route-aware `SystemBars` для Android/iOS и нижние inset карты/списков/шторок;
- получить решение руководителя по платёжной QR-кнопке: реализовать законченный сценарий либо временно скрыть неработающую кнопку;
- сохранение логина и пароля исключено; приложение хранит только Bearer-токен в Keychain/Keystore, а повторный вход может использовать серверную SSO-сессию;
- полноценные SSO, CAPTCHA и выбор кафе реализованы локально; для release-возврата SSO в приложение требуется production-развёртывание подготовленных Laravel-изменений;
- нижняя системная область карты в текущей светлой теме; тёмное состояние проверяется в отдельной задаче тем;
- визуальная сверка с сайтом выполнена владельцем на debug-эмуляторах;
- Android release проверен на реальном Samsung, iOS release — на Simulator;
- реальный iPhone остаётся проверкой руководителя;
- фиксация допустимых платформенных отличий.

## 5. Карта экранов

| Блок | Эталон сайта | Приложение | Статус |
| --- | --- | --- | --- |
| Стартовый экран | `/initial` | `Greeting.tsx` | завершён по UI |
| Авторизация | `/auth` | `AuthScreen.tsx` | UI, Laravel-вход, CAPTCHA и SSO реализованы; production backend ещё не развёрнут |
| Восстановление пароля | `/registration` | `ResetPwdScreen.tsx` | Laravel + CAPTCHA + 6 цифр реализованы; реальная доставка SMS остаётся внешней проверкой |
| Шапка / меню | `AppHeader` | `MainDrawerNavigator` / `CustomDrawerContent` | завершён в текущем светлом UI |
| Светлая / тёмная тема | `_app` / palette | `UiProviders` / UI config | отдельная задача после появления готового эталона на сайте |
| Список заказов | `/list_orders` | `OrdersListScreen` | реализован; debug smoke выполнен |
| Карточка заказа | `OrderCard` | `CardOrder` | реализована; debug smoke выполнен |
| Карта заказов | `/map_orders` | `MapScreen` | реализована; геолокация проверена в release на Samsung |
| Расчёт | `/price` | `PriceScreen` | реализован; debug smoke выполнен |
| График работы | `/graph` | `GraphScreen` | реализован; debug smoke выполнен |
| Статистика | `/statistics` | `StatisticsTableScreen` | реализована; доступность зависит от backend-флага `driver_page_stat_time` |
| Настройки | `/settings` | `SettingsScreen` | реализован с выбором кафе через Laravel; принят в debug |
| Обратная связь | `/feedback` | `FeedbackScreen` | реализована; debug smoke выполнен |

## 6. Известные доработки из плана Fabric

Эти пункты не должны потеряться во время визуального переноса:

1. [src/shared/ui/ScreenLayout.tsx](../src/shared/ui/ScreenLayout.tsx) переведён на `react-native-safe-area-context`; края drawer-экранов по умолчанию — `right/bottom/left`, без повторного `top`.
2. В [src/app/providers/AppProviders.tsx](../src/app/providers/AppProviders.tsx) подключён корневой `SafeAreaProvider` с `initialWindowMetrics`.
3. Auth, ResetPwd и Greeting используют верхний Safe Area, а экраны с drawer-шапкой его не дублируют.
4. Android и iOS используют единый route-aware `SystemBars`: светлые иконки над красной шапкой/Greeting и тёмные над Auth/ResetPwd; при глобальном затемняющем spinner иконки светлые.
5. Нижняя область карты согласована через `useSafeAreaInsets`; списки и обычные drawer-экраны защищены нижним краем общего layout, шторки используют собственный `bottomInset`.
6. При `night_map` карта тёмная, а общий UI приложения остаётся светлым.
7. Чекбоксы настроек карты после включения не выключались — исправлено в этапе 5.
8. После сохранения настройки должны обновляться в settings store — исправлено в этапе 5.
9. «Уведомить о решении» в обратной связи не выключался стабильно — исправлено в этапе 5.
10. Карта, drawer, авторизация, CAPTCHA и SSO проверены на iOS Simulator.
11. GPS-зависимый сценарий и повторное определение позиции проверены на реальном Samsung.
12. Release smoke выполнен на Samsung и iOS Simulator; реальный iPhone остаётся руководителю.

Фейковое удаление demo-аккаунта реализовано отдельным подпунктом Laravel-плана и проверено.

## 7. Процесс одного UI-этапа

1. Оркестратор выбирает один пункт этого плана.
2. Аналитик фиксирует источник, состояния, `In scope now`, `Out of scope` и критерии приёмки.
3. Пользователь отдельно разрешает изменения кода для этого пункта.
4. Разработчик выполняет только согласованный scope.
5. Тестировщик формирует короткий чек-лист состояний и рисков для проверки пользователем, но сам не запускает приложение и тесты.
6. Ревьюер статически сравнивает дифф со всем актуальным связанным кодом `jaco_driver_site` на ветке `api-laravel` и проверяет отсутствие придуманных или лишних изменений без запуска приложения.
7. Приложение, `lint`, `typecheck`, unit-тесты и ручной smoke Android/iOS после правок запускает пользователь.
8. Пользователь сообщает результат визуальной и технической проверки, после чего статус и оставшиеся отличия записываются в этот документ.
9. Коммит делает пользователь.

## 8. Минимальная матрица проверки экрана

Пользователь проверяет для каждого экрана по применимости:

- начальное состояние;
- заполненные поля или данные;
- loading;
- empty;
- error;
- длинный текст и увеличенный системный шрифт;
- небольшая высота и ширина экрана;
- открытая клавиатура;
- Safe Area сверху и снизу;
- Status Bar;
- светлая тема;
- тёмная тема;
- Android debug;
- iOS debug.

Точное визуальное сравнение выполняется с тем же состоянием сайта, а не только по памяти или по исходному SCSS.

## 9. Definition of Done UI-миграции

UI-миграция завершена, когда:

- все пункты карты экранов имеют финальный статус;
- ключевые состояния визуально сопоставлены с сайтом;
- согласованные отличия записаны;
- SSO и CAPTCHA реализованы и локально проверены; production backend должен быть
  отдельно настроен и развёрнут ответственным за сервер;
- существующая бизнес-логика не изменилась без отдельного согласования;
- Safe Area и Status Bar проверены на Android и iOS;
- светлая тема перенесена в рамках текущих экранов; полная светлая/тёмная тема ведётся отдельной задачей после готовности эталона сайта;
- решение руководителя по платёжной QR-кнопке зафиксировано и выполнено;
- решение по сохранению входа зафиксировано: логин и пароль не сохраняются,
  приложение хранит только Bearer-токен, повторный вход поддерживает SSO-сессию;
- пользователь подтвердил результат `lint`, `typecheck` и `test:unit`;
- пользователь подтвердил Android release smoke на реальном Samsung и iOS smoke
  на Simulator;
- реальный iPhone, QR и полная светлая/тёмная тема явно оставлены открытыми.

## 10. История статусов

| Дата | Изменение |
| --- | --- |
| 04.09.2026 | Мобильное приложение полностью переключено на Laravel API и Bearer-токен в Keychain/Keystore. Реализованы SmartCaptcha, SSO через системную auth-сессию/deep link, шестизначное восстановление, выбор кафе и фейковое удаление demo-аккаунта. Обновлены совместимые зависимости, зелёные `lint`, `typecheck`, 88 suites / 339 тестов. Android release установлен и проверен на реальном Samsung, включая исправленное повторное определение геопозиции; iOS проверен на Simulator. Открыты production-развёртывание CAPTCHA/SSO, реальный iPhone, QR и тема после сайта |
| 31.08.2026 | Выполнен кодовый аудит Safe Area и Status Bar без изменения бизнес-логики: общий layout переведён на `react-native-safe-area-context` без повторного верхнего inset под drawer-шапкой, Status Bar централизован для Android/iOS, подтверждены отдельные нижние inset карты и шторок, добавлены контрактные/UI-тесты. Успешно прошли `lint`, `typecheck` и полный `test:unit` — 79 suites / 274 теста; владелец проекта подтвердил debug smoke на эмуляторах Android/iOS, release smoke остаётся выполнить отдельно |
| 31.08.2026 | Перенесён UI страницы «Обратная связь»: фильтры, поиск, лента, карточки, FAB и нижние шторки создания/просмотра. Исправлен checkbox уведомления, сохранены legacy API и изображения; цвета и размеры всех статусов повторно сверены с текущим сайтом и закреплены unit/UI-тестами |
| 31.08.2026 | Перенесён и повторно сверен по Android/iOS-скриншотам UI страницы «Настройки»: компактные превью маркеров, единые radio/checkbox, параметры шрифта и масштаба, центрированные заголовки цвета, палитра из 18 оттенков и видимая кнопка сохранения. Legacy API сохранён; исправлено выключение чекбоксов и синхронизация settings store после сохранения. Фактический `getMySetting` не возвращает `point_id/all_points/points`, поэтому реальный выбор кафе и тёмная тема интерфейса оставлены до нового backend |
| 30.08.2026 | Перенесены UI страниц «График работы» и «Статистика» по актуальной ветке `api-laravel`: таблица смен, ошибки и их нижние шторки, выбор месяца, диапазон статистики, карточки курьеров и итогов. Legacy API/store сохранены; статистика временно открыта только в dev-сборке, добавлены собственные RN UI/hook-тесты |
| 30.08.2026 | Перенесён UI страницы расчёта по актуальной ветке `api-laravel`: единая карточка диапазона дат, итоговая сумма, строки показателей и сдач, подсказки и нижняя календарная шторка с диапазоном до 93 дней. Legacy API/store и аналитика сохранены; добавлены собственные RN UI/hook-тесты |
| 29.08.2026 | Фильтр активных заказов перенесён со старого `BottomSheet` и чекбоксов на общий `Actionsheet`: заголовок, подсказка, кнопки статусов, выбранное состояние и галочки соответствуют актуальному `OrdersFilterSheet`; `type_dop`, `setTypeDop`, аналитика и обновление заказов сохранены |
| 29.08.2026 | Исправлено пустое «Ко времени» на legacy API: фактический `api2` возвращает пустой `need_time`, поэтому добавлен ограниченный fallback из `time_start_mini` и верхней границы `unix_time_to_client`; готовый `need_time` всегда имеет приоритет. Проверки завершены успешно — 64 suites / 231 тест |
| 29.08.2026 | Телефоны карточки приведены к актуальному сайту: фон телефонных кнопок соответствует светлому `theme.palette.action.selected`, звонок другому курьеру использует `driver_login`, а номера из комментария поддерживают прямой звонок или нижнюю шторку выбора. Добавлен fallback `drink.names || drink.name`. Подтверждено, что платёжная QR-кнопка не имеет реализации ни в текущем коде, ни в доступной Git-истории |
| 29.08.2026 | Перенесены подтверждения «Отменить», «Завершить» и «Клиент не вышел на связь» из актуального `OrderConfirmModal.tsx` ветки `api-laravel`: тексты, иконки, цвета и нижняя шторка. Мобильные API/GPS/store-обработчики сохранены; добавлены собственные RN UI-тесты. Успешно прошли `lint`, `typecheck` и полный `test:unit` — 63 suites / 224 теста |
| 29.08.2026 | Кнопки карточки перенесены из актуального `OrderCard.tsx` ветки `api-laravel`: высота 44, радиус 8, интервал 8, подтверждённые цвета и компоновка свободного, своего и чужого заказа. Формат телефона не изменяется, API/GPS/store-обработчики сохранены |
| 29.08.2026 | Эталон UI изменён: используется всё текущее состояние проекта `jaco_driver_site` на ветке `api-laravel` без привязки к коммиту. Запрещено придумывать форматы тестовых данных, стили и поведение, которых нет в актуальном сайте |
| 29.08.2026 | Перенесён UI карточки заказа: поверхность, типографика, цветные теги, адресные, временные и денежные строки, комментарий и отменённое состояние. Логика кнопок, API и store не менялись; успешно прошли `lint`, `typecheck` и полный `test:unit` — 61 suite / 215 тестов |
| 29.08.2026 | Перенесён UI страницы списка заказов: сводка типа и лимитов, нижний селектор типа, loading, empty и pull-to-refresh. Карточка заказа, её действия, API и store не менялись. Добавлены UI/unit-сценарии; успешно прошли `lint`, `typecheck` и полный `test:unit` — 60 suites / 210 тестов |
| 29.08.2026 | Светлая и тёмная темы вынесены из текущей UI-миграции в отдельную задачу после подключения нового backend: мобильному приложению нужен контракт `dark_theme`, а в текущей ветке сайта отсутствует импортируемый `createAppTheme` |
| 28.08.2026 | Зафиксировано отличие backend-настроек: мобильный экран `Statistics` существует, но его пункт в drawer скрывается значением `driver_page_stat_time` от legacy API `api2`; после подключения `apidriver` нужно повторно проверить флаг, отображение пункта и переход на страницу |
| 28.08.2026 | Перенесены светлые UI шапки и бокового меню из `AppHeader`: бренд-зона, карточка среднего времени, разделы с активным состоянием, контакты, logout и кнопки меню/фильтра/обновления. Маршруты и действия сохранены. Добавлены 3 UI-теста; успешно прошли `lint`, `typecheck` и полный `test:unit` — 55 suites / 191 тест |
| 28.08.2026 | После переноса `AuthScreen`, `ResetPwdScreen` и `Greeting` обновлены и добавлены связанные unit/UI-тесты, расширены Jest-моки `Animated` и `ActivityIndicator`, исправлен тестовый жизненный цикл `useFocusEffect`. Проверки завершены успешно: `lint`, `typecheck`, 5 связанных suites / 22 теста и полный `test:unit` — 54 suites / 188 тестов |
| 28.08.2026 | Перенесён стартовый экран `Greeting`: вместо старого `wallpaper.png` используется фирменный красный фон `/initial`, крупный логотип, белый заголовок и индикатор проверки авторизации. Существующая проверка токена и навигация сохранены; приложение и тесты по договорённости не запускались |
| 28.08.2026 | По безопасным dev-логам подтверждено: мобильный клиент корректно отправлял на `api2` тот же номер и четырёхзначный код, однако legacy backend отвечал «Код из смс введен не верно». После перехода на Laravel API временные логи `[AUTH-RECOVERY]` удалены |
| 28.08.2026 | Исправлен повторный вход в восстановление пароля: при уходе с экрана полностью сбрасываются шаг, телефон, пароль, SMS-код, ошибка и видимость пароля, поэтому новый переход из авторизации всегда начинается с чистого первого шага |
| 28.08.2026 | SMS-код восстановления возвращён к 4 цифрам в соответствии с фактическим ответом текущего мобильного legacy API. После подключения нового `apidriver` ограничение необходимо снова изменить на 6 цифр |
| 28.08.2026 | Перенесён UI `ResetPwdScreen`: единая auth-карточка, stepper, требования сложного пароля, визуальная CAPTCHA без интеграции, один инпут SMS-кода, inline-подсказки и ошибки и ссылка возврата с анимацией. Старые глобальные модалки ошибок для `sendSMS`/`sendCode` отключены. Приложение и тесты по договорённости не запускались — проверку выполняет владелец проекта |
| 27.08.2026 | Перенесён UI `AuthScreen`: нативные поля и состояния, inline-ошибка, визуальные SSO/CAPTCHA без функционала, Safe Area/Status Bar, клавиатура, Roboto для iOS и test ID. Бизнес-логика API и токена сохранена. Android/iOS Debug, `lint`, `typecheck` и 183 unit-теста прошли; размеры и типографика оставлены на итоговую сверку владельцем проекта |
| 27.08.2026 | Создан отдельный план UI-миграции и доработаны роли Cursor, код приложения не менялся |
